#include <cs50.h>
#include <stdio.h>

int main(void)
{
    int n = get_int("n: ");

    // If n is even
    if (n % 2 == 0) // == is the equality operator, which means it is equal to the value.
    {
        printf("even\n");
    }
    // Else
    else (???)
    {
    printf("odd\n");
    }
}